## Expected Skill Behavior

Must include:

- paper-first reproduction workflow;
- evidence table for DOI, code archive, ASPECT version, commit/tag, plugins, data, and original `.prm`;
- use of `scripts/aspect-yuan reproduce catalog`;
- use of `scripts/aspect-yuan reproduce inspect`;
- use of `scripts/detect_aspect_reproduction_context.py`;
- isolated install/build plan using `references/aspect_installation_matrix.md` and `scripts/install_aspect_version.sh`;
- smoke test before full reproduction;
- explicit `unknown` labels where version evidence is missing.
