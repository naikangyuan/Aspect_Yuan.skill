# Rift Model Template

This template creates a beginner 2-D continental extension/rift case from the existing ASPECT teaching PRM.

Use:

```bash
aspect-yuan model create templates/models/rift/config.yaml
```

