# Subduction Model Template

This template creates a beginner 2-D kinematically driven subduction case. The current seed PRM is a teaching template and must be smoke-tested with the target ASPECT version before scientific use.

Use:

```bash
aspect-yuan model create templates/models/subduction/config.yaml
```

